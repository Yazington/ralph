# pnpm configuration for Ralph TODO app

# Pin exact versions for reproducibility in production builds
save-prefix=''

# Allow some flexibility with peer dependencies to avoid blocking on minor conflicts
strict-peer-dependencies=false

# Prefer using frozen lockfile for CI/CD consistency
prefer-frozen-lockfile=true
